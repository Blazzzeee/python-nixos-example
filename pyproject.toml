[project]
name = "fastapi-app"
version = "0.1.0"
description = "this is deployed via nixos"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.136.3",
    "uvicorn"
]

[tool.setuptools]
packages = ["app"]
